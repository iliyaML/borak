<br>
<main role="main" class="container">
  <div class="row">
        <div class="col-md-6 offset-md-3 col-sm-12">
            <!-- <h1 class="text-center">
                Borak
                <button id="clear" class="btn btn-danger">Clear</button>
            </h1> -->
            <div id="status"></div>
            <div id="chat">
              {{#if user}}
                <input type="text" id="username" class="form-control" placeholder="Enter name..." value="{{ user.id }}">
                {{/if}}
                <br>
                <div class="card">
                    <div id="messages" class="card-body" style="height:300px;">
                    </div>
                </div>
                <br>
                <textarea id="textarea" class="form-control" placeholder="Enter message..."></textarea>
            </div>
        </div>
    </div>
</main>
